# dotfiles
dotfiles! yuppers

TODO: Make a full install script. See `scratch.txt` for the setup process I noted down the last time I set up my computer

```
ln -s ./.i3_config ~/.config/i3/config

# Install themes first from https://github.com/alacritty/alacritty-theme
ln -s ./alacritty.toml ~/.config/alacritty/alacritty.toml

# NVIM is just ASTRONVIM with astroui set to astrolight LOL
```
